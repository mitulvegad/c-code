using System;
class dyarry
{
	static void main()
	{
		int[,] arr=new int [2,2];
		arr[0,0]=5;
		arr[0,1]=10;
		arr[1,0]=15;
		arr[1,1]=20;
	
		Console.WriteLine("total Element:" + arr.Length);
		
		for(int i=0;i<arr.GetLength(0);i++)
		{
			int i=0;i<arr.GetLength(1);i++)
			{
				Console.WriteLine(arr " + i +" "+ j + " = " + arr[i;j]);
			}
		}
	}
}